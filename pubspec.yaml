name: ideal
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '^2.18.0'

dependencies:
  flutter:
    sdk: flutter
  states_rebuilder:
  hive_flutter:
  uuid:
  file_picker: ^5.2.5
  google_fonts:
  colornames:
  shared_preferences: ^2.0.18
  equatable:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
    - lib/assets/fonts/
